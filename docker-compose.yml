services:
  research-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env          # never committed; see .env.example
    environment:
      LOG_LEVEL: INFO
    restart: unless-stopped
